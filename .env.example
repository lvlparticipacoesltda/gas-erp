# Local development
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/gas_erp?schema=public"
# Mesma URL localmente; no Neon use o host SEM "-pooler" (conexão direta para migrations)
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/gas_erp?schema=public"
JWT_SECRET="change-me-in-production-use-long-random-string"
JWT_EXPIRES_IN="7d"
API_PORT=3001
PORT=3001
WEB_URL="http://localhost:3000"
NEXT_PUBLIC_API_URL="http://localhost:3001/api/v1"

# Production (Fly.io API — GRU, recomendado)
# NEXT_PUBLIC_API_URL="https://api.thlgasdopovo.com.br/api/v1"
# ou temporário: https://gas-erp-api.fly.dev/api/v1

# Production (Railway API — legado)
# DATABASE_URL="postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require&connection_limit=10&pool_timeout=20"
# DIRECT_URL="postgresql://user:pass@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"
# SLOW_REQUEST_MS=1000
# SLOW_QUERY_MS=500
# PRISMA_LOG_QUERIES=false
# JWT_SECRET="<openssl rand -base64 48>"
# WEB_URL="https://thlgasdopovo.com.br"
# RESEND_API_KEY="re_xxxxxxxx"
# EMAIL_FROM="Gas ERP <noreply@thlgasdopovo.com.br>"
# NODE_ENV=production
# PORT is injected by Railway automatically

# Google Directions API (servidor — roteamento in-app do entregador)
# GOOGLE_MAPS_DIRECTIONS_API_KEY="AIza..."
